{
# The path to the bard sqlite database
    "database_path": "~/.local/share/bard/music.db",

# To use a postgresql database instead of sqlite, use the following variables
# and comment out the "database_path" entry:
#    "database": "postgresql",
#    "database_name" : "bard",
#    "database_user" : "bard",
#    "database_password" : "bard123",

# music_paths contains a list of paths where music is stored
    "music_paths": [
        "~/Music",
#       "~/Music2",
    ]
}
